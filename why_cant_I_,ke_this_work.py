#trying to count how many times c appears  in the text

def count_char(text, char):
    text="a rat in the house may eat the iced cream"
    char = c
    count = 0
    for c in text:
        if c == char:
            count += 1
        return (count)
        print("letter is not in string"):
        else:
    print(count)
  